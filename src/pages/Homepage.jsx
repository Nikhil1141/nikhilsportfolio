import React, { memo } from 'react'
import Animation from './Animation'

const Homepage = () => {
    return (
        <div>
            <Animation />
        </div>
    )
}

export default memo(Homepage)